# Managed by Ansible — linux_configure role (#696).
# audisp plugin: POST to EDA webhook on SELinux mode changes.
active = yes
direction = out
path = /usr/local/bin/eda-drift-reporter.sh
type = always
format = string
